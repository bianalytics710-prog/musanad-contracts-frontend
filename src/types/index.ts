/**
 * Public type re-exports for the M0 Foundation surface.
 *
 * Source-of-truth lives in api.types.ts (mirrored from the workspace
 * types.ts written by Agent 5 — Contract Generator). Feature modules
 * (M1+) will add their own type files alongside.
 */
export * from "./api.types";
