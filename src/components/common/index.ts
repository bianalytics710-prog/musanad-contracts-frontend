/**
 * Locked design-system / common-component re-exports.
 *
 * Feature modules should import from `@/components/common` rather than
 * the underlying file paths so we can tighten the surface later.
 */
export { AppLayout } from "./AppLayout";
